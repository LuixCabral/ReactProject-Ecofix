import React from 'react';
import '../../styles/Header.css';

const Header = () => {
    return(
        <header className='header'>
            <h1> NEWS FEED </h1>
        </header>
    );
};

export default Header;
